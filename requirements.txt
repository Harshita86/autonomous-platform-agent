# Thin vertical slice — minimal deps. LangGraph + Langfuse arrive when we widen.
httpx>=0.27
pydantic>=2.7
python-dotenv>=1.0

# LLM providers — install only the one(s) you use; the adapters import lazily.
anthropic>=0.40
openai>=1.40
google-genai>=0.3
